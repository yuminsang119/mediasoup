# ── 서버 공인 IP (WebRTC ICE candidate에 사용) ──
# 외부 접속을 위해 반드시 설정
PUBLIC_IP=203.0.113.100

# ── Docker Compose Profiles ──
# monitoring 프로필 활성화시 Prometheus + Grafana 포함
# docker compose --profile monitoring up -d
COMPOSE_PROFILES=
